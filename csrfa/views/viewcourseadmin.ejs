
<!DOCTYPE html>
<html>
<head>
    <title>Chapter List</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css">
</head>
<body class="bg-gray-100 p-12 md:p-16 lg:p-20 xl:p-24"> 
    <h1 class="text-3xl font-bold text-center my-6">Chapters</h1>
    <table class="table-auto w-full bg-white rounded-lg shadow-lg p-4"> 
        <thead>
            <tr class="bg-gray-200">
                <th class="px-4 py-2">Chapter ID</th> 
                <th class="px-4 py-2">Title</th> 
                <th class="px-4 py-2">Description</th> 
            </tr>
        </thead>
        <tbody>
            <% let i=0; chapters.forEach(function(chapter, index) {i++ %>
                <tr class="<%= index % 2 === 0 ? 'bg-gray-100' : 'bg-gray-200' %> hover:bg-gray-300">
                    <td class="px-4 py-2"><%= i %></td> 
                    <td class="px-4 py-2"><a href="/adminpage/<%= chapter.id %>"><div class="bg-green-500 hover:bg-green-700 text-white font-bold py-1 px-2 rounded">
                        <%= chapter.title %>
                      </div></a> <!-- Add an <a> tag with a link --></td> 
                    <td class="px-4 py-2"><%= chapter.description %></td> 
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>